rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /scholarships/{scholarshipId} {
      allow read: if true; // Anyone can read scholarships
      allow write: if false; // Only admin SDK (Functions) can write
    }
  }
}
